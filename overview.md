# 課題

### 問題文

存在しないルートにアクセスした際に404ページを表示してください。

`*` を使用してキャッチオールルートを実装します。

### 条件

1. `/` はHome、`/about` はAbout
2. 存在しないパスでは `404 Not Found`
3. `text-3xl font-bold text-red-600 text-center mt-10`
4. TypeScriptで作成する